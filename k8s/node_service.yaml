apiVersion: v1
kind: Service
metadata:
  name: streamlitapp
  labels:
    run: streamlitapp
spec:
  type: NodePort  
  ports:
    - nodePort : 30801
      port: 8502       
      targetPort: 8502    
      protocol: TCP
      name: http
  selector:
    app: streamlitapp
  externalIPs:
    - aws ip