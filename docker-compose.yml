version: '3.7'

services:
  streamlit:
    build: .
    image: mynsun/streamlitapp:latest
    container_name: streamlitapp
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./:/app
    restart: always